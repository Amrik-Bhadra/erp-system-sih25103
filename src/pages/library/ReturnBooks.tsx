import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const ReturnBooks = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Return Books</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Return book form or table */}
      </CardContent>
    </Card>
  );
};

export default ReturnBooks;
