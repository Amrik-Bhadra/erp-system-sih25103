import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const LibraryHome = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Library Home</CardTitle>
      </CardHeader>
      <CardContent>
        Welcome to the Student Library! Use the sidebar to navigate through different sections.
      </CardContent>
    </Card>
  );
};

export default LibraryHome;
