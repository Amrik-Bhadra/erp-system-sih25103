import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const LibraryNotices = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Library Notices</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Show latest library announcements */}
      </CardContent>
    </Card>
  );
};

export default LibraryNotices;
