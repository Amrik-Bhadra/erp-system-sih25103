import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const SearchBooks = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Search Books</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Add search input and filters here */}
      </CardContent>
    </Card>
  );
};

export default SearchBooks;
